import type { InventoryItem } from "./mock-data";

// Lighthouse starts with a deliberately empty beverage catalogue. Items and
// prices are created by authorized staff from the restored inventory screens.
export const BARISTA_INVENTORY_SEED: Partial<InventoryItem>[] = [];
